# dependencies (dev only)
node_modules/

# generated by tools/screenshots.mjs
shots/

# Android build output / local signing material
android/.gradle/
android/build/
android/app/build/
android/local.properties
# keystores stay local; only the public certificate is versioned (the CI needs
# it to sign every release with the same, frozen certificate — see
# android/keystore/README.md)
android/keystore/*
!android/keystore/README.md
!android/keystore/spotiduck.crt
*.jks
*.keystore
*.p12

# editor / OS
.DS_Store
Thumbs.db
*.log
